name: fake_call_detector
description: "On-device AI fake call detector for Android — no root required."
publish_to: 'none'

version: 0.5.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite_sqlcipher: ^3.4.0
  path: ^1.8.3
  intl: ^0.18.1
  flutter_secure_storage: ^9.2.2
  cryptography: ^2.7.0
  flutter_riverpod: ^2.6.1
  collection: ^1.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
